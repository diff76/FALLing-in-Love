# Supabase (public keys are safe in the browser; the secret key is server-only)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Public + Ops origins (used for absolute links such as the pass URL in QR codes)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_OPS_URL=http://localhost:3001
